# Per-run gate verdict logs are runtime artifacts — not committed.
# The Conductor's close-out step may snapshot a run's records into its
# run report (.orchestration/_templates/run-report.md); the live log is local.
*.jsonl

# Keep the directory and its schema doc tracked.
!.gitignore
!README.md
