# C4 L4 — Code

> Living description. See `README.md` for the index. **Stub** — to be authored by the Architecture Agent.
> L4 is authored selectively — only for code whose structure is load-bearing or non-obvious.

## Status

Stub (2026-06-13). Not yet authored.

## Key code elements

_TBD — the `packages/db` Prisma `$extends` wrapper and its `withClerkIdentity` helper (ADR-003), key
server actions / route handlers, and the concrete `Prisma<Entity>Repository` implementations (ADR-011)._

## Notes

Governing ADRs: ADR-003 (identity propagation), ADR-009 (signed-URL pre-signing path), ADR-011
(repository implementations).
