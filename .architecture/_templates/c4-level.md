# C4 L<N> — <Context | Containers | Components | Code>

> Living description. Updated in place as the system evolves. Keep `c4/README.md` (the index) consistent
> with this file. Cite the ADRs that drove each structural choice.

## Status

<Initial | Current as of YYYY-MM-DD>. <One line on what last changed and why.>

## <Diagram>

<ASCII or Mermaid diagram at this level. L1 = users + external systems + system boundary. L2 =
deployable/runnable containers and the protocols between them. L3 = components/modules within a
container. L4 = key classes, hooks, server actions, route handlers.>

## Elements

<One subsection per element at this level: its responsibility, the technology realizing it, and the
ADR(s) that govern it.>

## Relationships

<How the elements interact — calls, data flows, trust boundaries. Name the protocol/contract.>

## Notes

<Anything a reader needs to interpret the level correctly — deferred elements, planned changes, links to
the relevant ADRs and requirements.>
