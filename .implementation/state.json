{
  "schemaVersion": "1.0",
  "lastUpdated": "2026-06-26T20:08:50.068Z",
  "currentBrief": "BRIEF-018",
  "currentPhase": "Close-prep",
  "currentSliceDescription": "Email digest fallback — content-free daily nudge, accountant self-suppression, client default-on (EPIC-018)",
  "currentBranch": "brief-018-email-digest-fallback",
  "awaitingMerge": [],
  "openRetroItems": [
    {
      "id": "retro-012-001",
      "category": "CI",
      "description": "`test-portal` job lacks a `packages/**` build step** — graduate `test-portal` to required only after adding `pnpm -r --filter './packages/**' build --if-present` (HANDOFF-004 follow-up #3; the `@ta...",
      "note": "- **[CI — carried, now actionable] `test-portal` job lacks a `packages/**` build step** — graduate `test-portal` to required only after adding `pnpm -r --filter './packages/**' build --if-present` (HANDOFF-004 follow-up #3; the `@tax-portal/ui` failures pre-date EPIC-004, run `27568768517`; EPIC-004 extends the pattern to `@tax-portal/auth`). Tests pass locally (`pnpm -r test` 158/158).",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-002",
      "category": "infra",
      "description": "clean-volume bootstrap, Prisma `;port=` / `!`-password parsing, P3019 `mssql`-vs-`sqlserver`; why local Smoke is env-blocked (HANDOFF-004 follow-up #2).",
      "note": "- **[infra — carried] Local DB-bootstrap + `migrate deploy` P3019** — clean-volume bootstrap, Prisma `;port=` / `!`-password parsing, P3019 `mssql`-vs-`sqlserver`; why local Smoke is env-blocked (HANDOFF-004 follow-up #2).",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-003",
      "category": "gated-path-candidate",
      "description": "consider extending `packages/eslint-config` to also restrict `adminDb` imports outside sanctioned admin paths. (Observation; moved with the deferred Clerk-binding scope to the 2FA-enablement slice.)",
      "note": "- **[gated-path candidate — carried] ESLint import boundary covers only `requestDb`, not `adminDb`** — consider extending `packages/eslint-config` to also restrict `adminDb` imports outside sanctioned admin paths. (Observation; moved with the deferred Clerk-binding scope to the 2FA-enablement slice.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-004",
      "category": "gated-path-candidate",
      "description": "`adminDb` type-cast in `apps/admin/src/app/requests/[id]/page.tsx`** — the orphan-route nav fix (TASK-007-006) casts the admin db client to reach the engagement-by-request lookup. SDET-approved; ad...",
      "note": "- **[gated-path candidate — BRIEF-007 Audit Obs 6] `adminDb` type-cast in `apps/admin/src/app/requests/[id]/page.tsx`** — the orphan-route nav fix (TASK-007-006) casts the admin db client to reach the engagement-by-request lookup. SDET-approved; advisory. Add a typed accessor on the `packages/db` admin client so the cast is not needed; rides the next `packages/db` task that touches this surface. (Observation — not promoted; no gate failure.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-005",
      "category": "demo",
      "description": "pre-existing, transient/timing (HANDOFF-004 follow-up #6).",
      "note": null,
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-006",
      "category": "metric-integrity",
      "description": "`Started-at` midnight-sentinel placeholder** — TASK-002-003 AND BUG-002-004 each carry `Started-at: 2026-06-16T00:00:00Z` (a placeholder, not a real start; same pattern seen on several EPIC-004 tas...",
      "note": "- **[metric-integrity — BRIEF-002 Audit Obs 2] `Started-at` midnight-sentinel placeholder** — TASK-002-003 AND BUG-002-004 each carry `Started-at: 2026-06-16T00:00:00Z` (a placeholder, not a real start; same pattern seen on several EPIC-004 tasks). In range, metadata gate passed, no gate tripped. RETRO-002 item: the Dispatch-Checkpoint `Started-at` write should capture a real clock value, not a midnight sentinel. (Observation — no gate failure; not promoted.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-007",
      "category": "doc-drift",
      "description": "comments say \"real app uses `@read_only = 1`\" and cite \"ADR-003 §4 pool hygiene\", both factually wrong after BUG-002-003 / ADR-003 Amendment 1 dropped `@read_only`. **Functional behavior is correct...",
      "note": "- **[doc-drift — BRIEF-002 Audit Obs 3] stale test-helper comments in `packages/db/src/service.rls.test.ts` (~L70–72, ~L88)** — comments say \"real app uses `@read_only = 1`\" and cite \"ADR-003 §4 pool hygiene\", both factually wrong after BUG-002-003 / ADR-003 Amendment 1 dropped `@read_only`. **Functional behavior is correct — comment text only.** Disposition: **defer to a non-blocking follow-up** (lighter path; this is a comment-only doc-drift in a gated path, not a behavior defect — folding a micro-dispatch through the full pipeline to fix two comment lines is disproportionate). RETRO-002 will carry it as an `ungated`/follow-up note; the comment correction rides the next `packages/db` task that touches this file, OR a dedicated doc-drift cleanup if none arises. Recorded in HANDOFF-002 as a known comment-drift follow-up.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-008",
      "category": "process",
      "description": "app-scoped gates were used throughout (correct for per-task submission). Whether Validate/slice-close should require a full `ci:local` run is ambiguous. RETRO-002 candidate (the SDET CI gate at Val...",
      "note": "- **[process — BRIEF-002 Audit Obs 4] Full CI (`pnpm ci:local`) not recorded in any BRIEF-002 task Work Log** — app-scoped gates were used throughout (correct for per-task submission). Whether Validate/slice-close should require a full `ci:local` run is ambiguous. RETRO-002 candidate (the SDET CI gate at Validate is the natural home for a full `ci:local` if we want one). (Observation — no gate failure.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-009",
      "category": "rule-sunset",
      "description": "Autonomy Ceiling item 2 `--no-verify` clause + the `PushNotification` spam-loop guard, neither triggered in 3+ slices. Surface at Close-prep retro for a keep/remove recommendation per ENGINE.md § R...",
      "note": "- **[rule-sunset — BRIEF-002 Audit Obs 5] Sunset candidates** — Autonomy Ceiling item 2 `--no-verify` clause + the `PushNotification` spam-loop guard, neither triggered in 3+ slices. Surface at Close-prep retro for a keep/remove recommendation per ENGINE.md § Rule Sunset.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-010",
      "category": "infra",
      "description": "`sqlserver` compose status `(unhealthy)`** — the compose healthcheck runs `sqlcmd -U sa -P \"$MSSQL_SA_PASSWORD\"`, which fails (Error 18456 State 8) because `MSSQL_SA_PASSWORD` only takes effect at ...",
      "note": "- **[infra — BRIEF-002 Smoke, new manifestation of the carried infra item] `sqlserver` compose status `(unhealthy)`** — the compose healthcheck runs `sqlcmd -U sa -P \"$MSSQL_SA_PASSWORD\"`, which fails (Error 18456 State 8) because `MSSQL_SA_PASSWORD` only takes effect at volume-init and the persisted data volume was bootstrapped with a different SA password. **Not a BRIEF-002 regression; DB fully operational via app principals** (`taxportal_user`/`taxportal_admin` are SQL logins stored in the DB, not SA-gated) — all EPIC-002 DB objects verified present in-container (`sec.fn_service_write_access`; `pol_Service` 4 BLOCK→write predicate + 1 FILTER→read predicate; `taxportal_user` principal). **Non-blocking for Validate.** RETRO-002: the healthcheck should derive its SA password from the same source the volume was bootstrapped with (or the bootstrap should re-assert the env SA password on persisted volumes). Same root family as the carried P3019/bootstrap-fragility infra item — record as a new manifestation, not a new class.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-011",
      "category": "ungated-fix",
      "description": "`scripts/smoke-test.sh` defaults unsuitable for this project** — the script defaults `ADMIN_URL=http://localhost:3001` (not the project-remapped `:13001`) and its `sqlserver` readiness wait uses `s...",
      "note": "- **[ungated-fix candidate — RETRO-006 item 3] `scripts/smoke-test.sh` defaults unsuitable for this project** — the script defaults `ADMIN_URL=http://localhost:3001` (not the project-remapped `:13001`) and its `sqlserver` readiness wait uses `sqlcmd -U sa -P \"$MSSQL_SA_PASSWORD\"`, which blocks on the carried SA-password/volume mismatch. SDET used the CLAUDE.md manual fallback at BRIEF-006 Smoke. Harden: default `ADMIN_URL` to `:13001`; derive/re-assert the SA password from the volume bootstrap source. Same root family as the `sqlserver` healthcheck item above. (Observation — not promoted; no gate failure.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-012",
      "category": "ungated-fix",
      "description": "`@demo` prior-epic PNG byte-churn** — `@demo` runs rewrite prior-epic PNGs (3rd-ish occurrence; main session manually `git checkout`-reverts each slice). TASK-006-007 itself was scope-disciplined (...",
      "note": "- **[ungated-fix candidate — RETRO-006 item 4] `@demo` prior-epic PNG byte-churn** — `@demo` runs rewrite prior-epic PNGs (3rd-ish occurrence; main session manually `git checkout`-reverts each slice). TASK-006-007 itself was scope-disciplined (only EPIC-006 PNGs written); this concerns the *other* `@demo` specs' default output paths. Scope each `@demo` spec's screenshot output to its own `docs/demos/EPIC-NNN/` path so the manual revert isn't needed. (Observation — not promoted.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-013",
      "category": "e2e-determinism",
      "description": "`apps/portal/e2e/specs/onboarding.spec.ts:312` failed once at single-run in the full portal suite (`data-testid=\"onboarding-steps\"` not found within 5000ms), passed on `--retries 1` at 259ms. File ...",
      "note": "- **[e2e-determinism — RETRO-006 item 5] AC-ONBD-001-01 (EPIC-005-owned) portal e2e flake** — `apps/portal/e2e/specs/onboarding.spec.ts:312` failed once at single-run in the full portal suite (`data-testid=\"onboarding-steps\"` not found within 5000ms), passed on `--retries 1` at 259ms. File unmodified this branch (last touched `f879da2`/EPIC-005). Not a BRIEF-006 regression, not a BRIEF-006 AC. Investigate the `beforeEach`/onboarding-nav fixture timing for that describe block. (Non-blocking follow-up.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-014",
      "category": "metric-integrity",
      "description": "recurred again on **TASK-008-002** (`Completed-at 17:17` < `Started-at 22:09`) and **TASK-008-003** (`Completed-at 17:40` < `Started-at 22:27`); prior at TASK-007-001..004. Developer agents wrote `...",
      "note": "- **[metric-integrity — RETRO-006 item 2 → ELEVATED at BRIEF-007 → 9th+ recurrence at BRIEF-008] Clock-source `Completed-at`/`Started-at` inversion → `ungated-fix`** — recurred again on **TASK-008-002** (`Completed-at 17:17` < `Started-at 22:09`) and **TASK-008-003** (`Completed-at 17:40` < `Started-at 22:27`); prior at TASK-007-001..004. Developer agents wrote `Completed-at` in a later clock session against an earlier `Started-at` instead of leaving it blank for the SDET's atomic close. This is the **9th+ occurrence** project-wide (RETRO-002/003/004/005/006 + BRIEF-007 + BRIEF-008). **Disposition: `ungated-fix`** (Overwatch-recommended; IO-classified). **Fix:** amend `.implementation/agents/developer.md` (and/or the Dispatch-Checkpoint guidance) to **prohibit developer writes to `Completed-at`** — per the Task Metadata Contract, `Completed-at` is SDET-authored (or IO-as-reviewer for `Impl: io`) inside the atomic close edit only. **This is an ungated-path doc edit (quad-review workflow-file change); it does NOT ride the BRIEF-008 PR** — it rides a future docs/ungated change. Tracked here until that change lands. **Fold in the sister `Updated-by`-staleness finding (RETRO-008 item 1a):** all 5 BRIEF-008 tasks were left `Updated-by: webapp-developer`, not flipped to `sdet` on the atomic close — the same close-edit fix should set `Updated-by: sdet` alongside the `Completed-at` write.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-015",
      "category": "gated-path-candidate",
      "description": "3 advisory `onboarding-completion.*` cleanups** — carried from the PR #55 review (panel APPROVE; fix-decision SKIP). Ride the next `packages/db` task that touches `onboarding-completion.*`: (1) **f...",
      "note": "- **[gated-path candidates — PR #55 panel minors, deferred 2026-06-20] 3 advisory `onboarding-completion.*` cleanups** — carried from the PR #55 review (panel APPROVE; fix-decision SKIP). Ride the next `packages/db` task that touches `onboarding-completion.*`: (1) **fire-once concurrency test gap** (`onboarding-completion.integration.test.ts:507` — the test covers the `status !== 'New'` short-circuit but not the `@@ROWCOUNT`-guard concurrent path; add a concurrent-completion test asserting exactly one transition + one notification); (2) **unused export `ENGAGEMENT_TRANSITION_ACTION`** (`onboarding-completion.ts:74` — drop the export/barrel re-export or wire its consumer); (3) **redundant `EngagementWithClient` interface + field-by-field copy** (`onboarding-completion.ts:79` + copy 172–180 — collapse to the shared type). Full detail + the 2 dispositioned-as-intended items in `RETRO-008.md § Post-Merge Addendum`. (Observation — no gate failure; not slice-blocking.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-016",
      "category": "gate-design",
      "description": "`check_work_log_content` wording brittleness + late-firing-on-`done`** — `scripts/validate-gates.sh`'s literal `\"Starting implementation\"` substring grep rejected TASK-008-002's truthful synonym \"S...",
      "note": "- **[gate-design — RETRO-008 item 3, observation] `check_work_log_content` wording brittleness + late-firing-on-`done`** — `scripts/validate-gates.sh`'s literal `\"Starting implementation\"` substring grep rejected TASK-008-002's truthful synonym \"Starting TDD\" (fixed at gate-7 by the IO `Impl: io` mechanical Work-Log-wording alignment, commit `06119e2`, NOT fabrication). **Recommend:** broaden the grep to a synonym set (`Starting (implementation|work|TDD|coding)|Beginning implementation`) OR publish the exact required phrase to developers in the Dispatch-Checkpoint guidance. Secondary: the check fires only once a task flips to `done` (skipped at WIP/`review`), so the miss surfaced late at the de-WIP'd-PR CI rather than at the per-task submission gate — consider running it at submission time too. ENGINE/CI-tooling backlog; not slice-blocking.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-017",
      "category": "acknowledged",
      "description": "RESOLVED by BRIEF-LOE-013 (Close-prep 2026-06-22). Cross-layer removal-sweep gate shipped: `scripts/validate-gates.sh` check 10 (`check_removed_artifact_orphans`) — diff-based, un-forgettable detection; executable-fail / doc-allow classification; reason-mandatory allowlist (`.implementation/removal-sweep-allow.txt`); SKIP-not-FAIL on no diff base. ENGINE.md § Removal Sweep standing rule binds every removal task across EVERY layer. AC-04 proves real main stays green (the .orchestration/ retentions are allowlisted-with-reason). Closure recorded in RETRO-LOE-013.md.",
      "note": "- **[RESOLVED — BRIEF-LOE-013, resolvedBy=BRIEF-LOE-013, resolvedAt=2026-06-22] Cross-layer removal-sweep gate.** The BRIEF-LOE-012 panel MAJOR (orphan-after-removal — PROGRESS.md retirement missed two LIVE `.orchestration/` Conductor consumers) is now operationalized as a task-time gate. check 10 catches the exact PR #80 failure mode (a constructed-path `.sh` consumer of a removed file) at task-time instead of review-time; the red counterfactual fixture reproduces it (exit 1, names the consumer). See RETRO-LOE-013.md for the AC↔evidence map and the two SDET-adjudicated judgment calls. (Category `acknowledged` = resolved/known per the state.json schema enum.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-013-001",
      "category": "acknowledged",
      "description": "RESOLVED — FIXED AT SOURCE in PR #83 (commit b442a9e, squashed into da285f3). The self-referential-allowlist concern (SDET judgment-call-2) was eliminated, not merely watched: the real-repo sweep now excludes `scripts/__test_fixtures__/`, `*.test.ts`, and `*.spec.ts` by pathspec (real-repo git grep branch ONLY — fixture mode untouched, red fixtures still red); the validate-gates.sh self-doc comments were reworded to a generic `<removed-artifact-path>` placeholder; and the allowlist was pruned from 10 entries (6 self-referential) back to the 4 genuine `.orchestration/` cross-layer retentions only (the legacy --progress-md no-op flag in orchestrate-state.sh / orchestrate-gates.sh / sequence.sh / sequence.test.sh). The allowlist now means 'intentional cross-layer retentions,' not a test-scaffolding dumping ground. AC-04 re-verified green; 293/293 scripts tests; check 8 byte-unchanged. The exclusions are a documented, scoped decision (real-tree branch only — cannot leak into fixture mode), not a silent-suppression class.",
      "note": "- **[RESOLVED — BRIEF-LOE-013, resolvedBy=PR#83/b442a9e, resolvedAt=2026-06-22] Self-referential allowlist-ratio concern fixed at source.** The user-directed post-review allowlist-hygiene refinement excluded test scaffolding (`scripts/__test_fixtures__/` + `*.test.ts`/`*.spec.ts`) from the REAL-tree sweep only, reworded the self-doc comments to a placeholder, and pruned the allowlist to the 4 genuine `.orchestration/` retentions. The ratio-watch is moot — zero self-referential entries by construction. (Category `acknowledged` = resolved per the state.json schema enum.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-013-002",
      "category": "observation",
      "description": "File-level-only removal-sweep scope (deliberate boundary, documented). check 10 (`check_removed_artifact_orphans`) detects FILE deletions only — symbol/section/field-level removal (a removed exported symbol, a deleted doc section, a dropped config key) is OUT OF SCOPE by design (file deletes are deterministic + low-noise). This is a deliberate future extension, not an oversight; the boundary is noted in the check comment (validate-gates.sh:704-707) and ENGINE.md § Removal Sweep. Carry as a known scope boundary should a future orphan-after-symbol-removal incident motivate finer-grained detection. Observation; no action this slice.",
      "note": "- **[gate-scope — RETRO-LOE-013] File-level-only removal-sweep boundary.** The gate is file-level by design; symbol/section/field-level removal detection is a deliberate deferred extension. Documented in-code + in ENGINE.md. Tracked so a future symbol-orphan incident has a recorded starting point.",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-012-018",
      "category": "observation",
      "description": "CS-INFRA-005 (atomic-write temp+rename) drafted experimental during BRIEF-LOE-012 Standards audit; left UNRATIFIED/UNCOMMITTED awaiting human ratification per the .code-standards/ governance (machine proposes by:agent, human ratifies by:user).",
      "note": "- **[code-standards — BRIEF-LOE-012 Standards audit, awaits human ratification] CS-INFRA-005 atomic-write standard.** The Standards-review audit (APPROVE, 0 violations) drafted an experimental `CS-INFRA-005` (atomic write via temp file + `fs.renameSync`, the pattern the 5 `task.ts` state-store commands use) but left it **unratified and uncommitted** at `.code-standards/standards/infra/CS-INFRA-005-atomic-write-temp-rename.md`. Per `.code-standards/` governance, promotion is machine-proposes (`by: agent`) / human-ratifies (`by: user`) -- this item tracks the pending human ratification decision. Do NOT auto-ratify. (Observation -- no gate failure.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-013-003",
      "category": "observation",
      "description": "CS-INFRA-006 (gate-check SKIP-not-FAIL on optional input) drafted experimental during the BRIEF-LOE-013 Standards audit (APPROVE, 0 violations); left UNRATIFIED/UNCOMMITTED awaiting human ratification per .code-standards/ governance (machine proposes by:agent, human ratifies by:user). The pattern: check 10 (and check 8) SKIP cleanly rather than FAIL when their optional input is absent (no diff base / empty removal set) — never a false PASS, never a hard error. Do NOT auto-ratify.",
      "note": "- **[code-standards — BRIEF-LOE-013 Standards audit, awaits human ratification] CS-INFRA-006 SKIP-not-FAIL-on-optional-input.** The Standards-review audit (APPROVE, 0 violations) machine-proposed an experimental `CS-INFRA-006` capturing the removal-sweep gate's SKIP-not-FAIL discipline (a gate check whose input is optional must SKIP, not FAIL or falsely PASS, when that input is absent) but left it **unratified and uncommitted** at `.code-standards/standards/infra/CS-INFRA-006-gate-check-skip-not-fail-on-optional-input.md` (untracked, `??`). Per `.code-standards/` governance: machine proposes (`by: agent`) / human ratifies (`by: user`). Do NOT auto-ratify. (Observation -- no gate failure.)",
      "addedAt": "2026-06-22"
    },
    {
      "id": "retro-017-pre01",
      "category": "acknowledged",
      "description": "Pre-existing (EPIC-016 #102 / carried BUG-007-001 mock-scanner-env): 2 failures in packages/db/src/document.upload-pipeline.rls.test.ts. Independently isolation-proven pre-existing (RETRO-016 lesson) by git-stash rerun on multiple BRIEF-017 tasks + empty branch diff for the file vs main + Overwatch Audit Finding 3. NOT a BRIEF-017 regression. Latent on main because `pnpm -r test` is not a required CI check. WILL surface at the SDET CI gate (`pnpm -r test`) — IO verified scope at Audit and acknowledges it as a pre-existing main item (mock-scanner ALLOW_MOCK_SCANNER env in the upload-pipeline test), to be dispositioned at the SDET CI gate (expected-known-failure) not fixed-forward in this slice unless scope proves mechanical. Sibling to BUG-017-001 (the EPIC-016 build break this slice DID fix forward).",
      "note": "- **[acknowledged — BRIEF-017 Audit Finding 3 / carried] Pre-existing 2 `document.upload-pipeline.rls.test.ts` failures.** Confirmed pre-existing (not introduced by BRIEF-017): git-stash isolation on TASK-017-001/-002/-004/-005/-007 reruns + empty `git diff main...HEAD` for the file + Overwatch Audit Finding 3. Root family: the mock-scanner env (carried BUG-007-001 / retro-012 infra family). Latent on `main` because `pnpm -r test` is advisory, not required. Disposition: acknowledged pre-existing main item; the SDET CI gate treats it as a known-pre-existing failure (not a BRIEF-017 reject). Fix-forward NOT taken (different root cause than this slice's work; would expand scope). If a future slice's CI maturation (retro-012-001) graduates `pnpm -r test`, this gets a dedicated BUG. (Category `acknowledged` per the state.json schema enum.)",
      "addedAt": "2026-06-25"
    },
    {
      "id": "retro-017-01",
      "category": "e2e-determinism",
      "description": "General-thread new-message notification LINK lacks e2e coverage (advisory). The general-thread link path (linkedItemType:'thread' → /messages/<threadId>, BUG-017-002 Option A renderer) is covered by UNIT tests on both surfaces; the engagement-thread link path is covered by e2e. Add a general-thread e2e link assertion in a follow-up. Non-blocking; do NOT re-open BRIEF-017. Advisory — no gate failure.",
      "note": "- **[e2e-determinism — RETRO-017, advisory] General-thread notification-link e2e gap.** BUG-017-002 fixed the masked notification-link defect with the row-only renderer (engagement → linkedItemType:'engagement'/engagementId; general → linkedItemType:'thread'/threadId → /messages/<threadId>). The general-thread branch is proven by unit tests on both surfaces; the engagement-thread branch is proven by e2e. Follow-up: add a general-thread e2e link-click assertion so both renderer branches have e2e coverage. Rides a future messaging-surface task; do NOT re-open this slice. (Observation — no gate failure; not promoted.)",
      "addedAt": "2026-06-25"
    }
  ]
}
